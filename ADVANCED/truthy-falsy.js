const products = ['item']
const product = products[0]

//Every single value in javascript will evaluate to either truthy or falsy

//Truthy - values that resolve to truthy in boolean context (like in an if statement check)
//FAlsy - values that resolve to falsy in a boolean content
    //Falsy values - false, 0, empty string, null, NaN, undefined
if (null) {
    console.log('Product found')
} else {
    console.log('Product not found')

}

//Everything else is truthy

